name: Deploy Marvel a Hostinger (FTP)

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      motivo:
        description: "Motivo del deploy"
        required: false
        default: "Deploy manual"

jobs:
  deploy_app:
    name: Deploy app principal
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Generar bundle size (JS/CSS)
        run: php bin/generate-bundle-size.php

      - name: Crear archivo prueba (APP)
        run: |
          cat > DEPLOY_TEST_APP.txt <<EOF
          Servicio: Clean Marvel Album (APP principal)
          Fecha/hora (UTC): $(date -u '+%Y-%m-%d %H:%M:%S')
          Mensaje: Si ves este archivo, el deploy funciona.
          EOF

      ####################################################################
      # 1) Deploy APP PRINCIPAL (server-dir: iamasterbigschool)
      ####################################################################
      - name: Deploy app principal (iamasterbigschool)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          protocol: ftp
          port: ${{ secrets.FTP_PORT }}

          # 📌 RUTA REAL DEL PROYECTO COMPLETO EN HOSTINGER
          server-dir: /domains/contenido.creawebes.com/public_html/iamasterbigschool/

          # 📌 Subimos TODO el proyecto (NO solo public)
          local-dir: ./

          # ✅ Estado aislado (evita colisiones entre despliegues)
          state-name: ftp-state-app.json

          # 📌 Archivos/carpetas que NO deben subirse
          exclude: |
            **/.git*
            **/.github/**
            node_modules/**
            tests/**
            .scannerwork/**
            coverage.xml
            pa11y-*.sh
            package-lock.json
            package.json
            playwright.config.cjs
            test-results/**
            blob-report/**
            playwright-report/**
            openai-service/**
            rag-service/**

  deploy_openai:
    name: Deploy openai-service
    runs-on: ubuntu-latest
    needs: deploy_app

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Crear archivo prueba (OPENAI)
        run: |
          cat > openai-service/DEPLOY_TEST_OPENAI.txt <<EOF
          Servicio: openai-service
          Fecha/hora (UTC): $(date -u '+%Y-%m-%d %H:%M:%S')
          Mensaje: Si ves este archivo, el deploy funciona.
          EOF

      ####################################################################
      # 2) Deploy microservicio OPENAI
      ####################################################################
      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add SSH known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p "${{ secrets.SSH_PORT || 22 }}" -H "${{ secrets.SSH_HOST || secrets.FTP_HOST }}" >> ~/.ssh/known_hosts

      - name: Deploy microservicio openai-service (SSH + rsync)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST || secrets.FTP_HOST }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          SSH_USER: ${{ secrets.SSH_USER || secrets.FTP_USER }}
          REMOTE_DIR: /domains/contenido.creawebes.com/public_html/openai-service/
        run: |
          rsync -az \
            --timeout=60 \
            --exclude ".git*" \
            --exclude ".github/" \
            --exclude "node_modules/" \
            --exclude "tests/" \
            --exclude ".scannerwork/" \
            --exclude "coverage.xml" \
            --exclude "test-results/" \
            --exclude "blob-report/" \
            --exclude "playwright-report/" \
            -e "ssh -p ${SSH_PORT} -o ServerAliveInterval=15 -o ServerAliveCountMax=4 -o ConnectTimeout=30" \
            ./openai-service/ \
            "${SSH_USER}@${SSH_HOST}:${REMOTE_DIR}"

  deploy_rag:
    name: Deploy rag-service
    runs-on: ubuntu-latest
    needs: deploy_openai

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Crear archivo prueba (RAG)
        run: |
          cat > rag-service/DEPLOY_TEST_RAG.txt <<EOF
          Servicio: rag-service
          Fecha/hora (UTC): $(date -u '+%Y-%m-%d %H:%M:%S')
          Mensaje: Si ves este archivo, el deploy funciona.
          EOF

      ####################################################################
      # 3) Deploy microservicio RAG
      ####################################################################
      - name: Deploy microservicio rag-service
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          protocol: ftp
          port: ${{ secrets.FTP_PORT }}

          server-dir: /domains/contenido.creawebes.com/public_html/rag-service/
          local-dir: ./rag-service/

          # ✅ Estado aislado (evita colisiones entre despliegues)
          state-name: ftp-state-rag.json
