apiVersion: v1
kind: Service
metadata:
  name: openai-service
  labels:
    app: openai-service
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: openai-service
    tier: backend
  ports:
    - name: http
      port: 8081
      targetPort: 8081
