# TFM Audit Snapshot - 2026-02-13

Este documento deja una evidencia reproducible del estado técnico del repositorio en la fecha indicada.

## Comandos ejecutados (local)

1. Conteo de tests (`*Test.php` en app + microservicios):

```bash
find tests rag-service/tests openai-service/tests -type f -name "*Test.php" | wc -l
```

Resultado:

```text
151
```

2. Ejecución de test suite con cobertura:

```bash
composer test:coverage
```

Resultado resumido:

```text
Tests: 656, Assertions: 1448, Warnings: 1, Skipped: 4.
OK, but there were issues!
```

3. Cobertura total desde `coverage.xml` (src):

```bash
php -r '$x=simplexml_load_file("coverage.xml"); $m=$x->project->metrics; $s=(int)$m["statements"]; $c=(int)$m["coveredstatements"]; $p=$s?($c/$s*100):0; echo "covered={$c} statements={$s} pct=".number_format($p,2)."%\n";'
```

Resultado:

```text
covered=4247 statements=4778 pct=88.89%
```

4. Conteo de ADR:

```bash
find docs/architecture -type f -name 'ADR-*.md' | wc -l
```

Resultado:

```text
18
```

5. Commits y versionado:

```bash
git rev-list --count HEAD
git tag --sort=-creatordate | head -n 10
```

Resultado resumido:

```text
577 commits
Tags recientes: v1.3.2, v1.3.1, v1.3.0, v1.2.0, ...
```

## Notas de interpretación

- Cobertura y número de tests son consistentes con una base de código madura para contexto académico.
- Los valores deben actualizarse si cambia el estado del repositorio.
